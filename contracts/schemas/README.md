# Contract Schemas

Run this command to export current Pydantic contracts as JSON Schema:

```powershell
.\.venv\Scripts\python.exe -m app.contracts.export_schemas
```

Schemas are generated artifacts and should be reviewed before publishing as external contracts.

